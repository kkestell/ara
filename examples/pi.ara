module main

fn pi(n: int) -> float {
  var pi: float = 4.0
  var flag: bool = false
  var i: float = 3.0

  for c in 3..n {
    if flag {
      pi = pi + 4.0 / i
      flag = false
    } else {
      pi = pi - 4.0 / i
      flag = true
    }
    i = i + 2.0
  }

  return pi
}
